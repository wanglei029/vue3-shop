/* 对获取state做一些映射 */
export const goods = (state: any) => state.goods
export const browsingList = (state: any) => state.browsingList
export const favoriteList = (state: any) => state.favoriteList
export const user = (state: any) => state.user
export const searchHistory = (state: any) => state.searchHistory
